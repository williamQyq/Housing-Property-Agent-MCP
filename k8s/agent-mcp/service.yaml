apiVersion: v1
kind: Service
metadata:
  name: agent-mcp-service
  labels:
    app: agent-mcp-service
spec:
  selector:
    app: agent-mcp-service
  ports:
  - protocol: TCP
    port: 8083
    targetPort: 8083
  type: ClusterIP
